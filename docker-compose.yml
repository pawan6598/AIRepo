version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - '8000:8000'
    env_file:
      - backend/.env.example
  frontend:
    build: ./frontend
    ports:
      - '3000:3000'
    environment:
      NEXT_PUBLIC_BACKEND_URL: http://backend:8000
    depends_on:
      - backend
